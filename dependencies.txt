conda create -n SurgicalGaussianSurfels python=3.9
conda activate SurgicalGaussianSurfels
conda install cudatoolkit=11.8
conda install nvidia/label/cuda-11.8.0::cuda-toolkit
pip install plyfile==0.8.1
pip install torch==2.1.2 torchvision==0.16.2 torchaudio==2.1.2 --index-url https://download.pytorch.org/whl/cu118
pip install tqdm
pip install -e submodules/diff-surfel-rasterization --use-pep517 --no-build-isolation
pip install -e submodules/simple-knn --use-pep517 --no-build-isolation
pip install -e submodules/fused-ssim --use-pep517 --no-build-isolation
pip install "git+https://github.com/facebookresearch/pytorch3d.git" --no-build-isolation
pip install matplotlib
pip install scikit-image
pip install mmcv-full
pip install pymeshlab
pip install ninja
pip install ninja git+https://github.com/NVlabs/tiny-cuda-nn/#subdirectory=bindings/torch
pip install h5py
pip install pytorch-lightning
pip install imageio==2.27.0
pip install opencv-python
pip install imageio-ffmpeg
pip install scipy
pip install dearpygui
pip install lpips
pip install timm
pip install tensorboard
pip install open3d
pip install --force-reinstall -v "numpy==1.25.2"

# Tiny cuda Option 1: Install from GitHub
pip install git+https://github.com/NVlabs/tiny-cuda-nn/#subdirectory=bindings/torch

# Tiny cuda Option 2: Install from local clone (if you have the submodule)
cd submodules/tiny-cuda-nn/bindings/torch
python setup.py install
